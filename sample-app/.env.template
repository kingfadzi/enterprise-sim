# App Intent Declaration - what the app needs the platform to know
# The platform handles all infrastructure concerns (namespaces, gateways, TLS, routing, etc.)

APP_NAME=hello-app
REGION=ap

# Storage Intent (optional)
# Enable persistent storage and install storage platform: ./enterprise-sim.sh storage up
STORAGE_PERSISTENT_ENABLED=true
STORAGE_PERSISTENT_SIZE=1Gi
STORAGE_PERSISTENT_CLASS=enterprise-standard

# Object Storage Intent (optional)
# Enable S3-compatible object storage and install MinIO platform: ./enterprise-sim.sh minio up
S3_ENABLED=true
S3_BUCKET_NAME=app-data
