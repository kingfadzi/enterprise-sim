apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: $vs_name
  namespace: $namespace
  labels:
    app: $app_name
    region: $region
    component: routing
spec:
  hosts:
  - $host
  gateways:
  - istio-system/$gateway_name
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: $service_host
        port:
          number: $service_port
